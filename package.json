{
  "name": "racefacer-lap-scraper",
  "version": "1.0.0",
  "description": "",
  "main": "src/app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "deno --allow-all --env-file src/app.ts",
    "lint": "deno lint",
    "format": "deno fmt"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^23.0.2"
  }
}
